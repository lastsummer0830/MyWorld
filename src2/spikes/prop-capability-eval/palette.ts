// AJP-004 · prop capability eval — 파스텔 정원 소품 팔레트.
//
// 규칙(/myworld-procedural-modeling §2):
//   - 파스텔 한 가족 안에서 **값(명도)** 을 나눈다. 색상만 바꾸면 형태가 아니라 무늬가 된다.
//   - 금속 반사 없음. 물뿌리개는 무광 도장으로 읽혀야 파스텔 정원과 같은 재질 언어가 된다.
//   - 강조색은 하나뿐이다(accent). 어깨 몰딩과 장미머리 테두리에만 쓴다.

export const CAN = {
  /** 몸통 주면 — 파스텔 민트. 이 값이 기준이고 나머지는 여기서 위아래로만 움직인다. */
  body: '#c3ddd1',
  /**
   * 아랫배(굽 끝 → 최대폭) — 배와 굽 사이의 중간 단.
   * 1차 캡처에서 굽 한 줄만으로는 30° 부각에서 배에 가려 바닥이 전혀 무겁지 않았다.
   * 한 단씩 내려가는 세 단이어야 값이 줄무늬가 아니라 무게로 읽힌다.
   */
  bodyBase: '#aecdc0',
  /** 굽·바닥띠 — 가장 어두운 같은 색상. 아래가 무거워야 물건이 땅에 붙는다. */
  bodyDeep: '#93b5a9',
  /** 아가리 롤 — 가장 밝은 면. 위쪽 모서리가 빛을 받는 방향을 값으로 못 박는다. */
  lip: '#e2f1ea',
  /** 안쪽 벽 — 구멍이 구멍으로 읽히려면 바깥보다 확실히 어두워야 한다. */
  inner: '#5f7a74',
  /** 손잡이·주둥이 = 따뜻하게 물들인 뉴트럴. 몸통과 다른 부재라는 걸 값과 온도로 알린다. */
  hardware: '#f1e8db',
  /** 접합 칼라·리벳 패드 — 하드웨어의 어두운 단. 접합부가 그림자로 읽힌다. */
  hardwareDeep: '#cfbfa9',
  /**
   * 유일한 강조색 — 살구빛 코랄.
   * 1차 캡처에서 어깨 띠가 화면에서 가장 센 요소라 몸통을 두 동강으로 잘랐다.
   * 폭·돌출을 줄이면서 채도도 한 단 낮춰 강조가 형태를 이기지 않게 한다.
   */
  accent: '#e2a795',
  /** 물구멍 — 뚫린 것으로 읽히게만 하는 어두운 뉴트럴. */
  hole: '#57504a',
} as const;

/**
 * shaded 무대. 파스텔 몸통보다 배경을 한 단 낮춰야 실루엣이 배경에 먹히지 않는다.
 * 1차 캡처에서 바닥(#c9d3c9)이 몸통(#c3ddd1)과 거의 같은 값이라 front/back 뷰에서
 * 물건 밑동이 바닥에 녹아 접지가 안 읽혔다. 바닥을 몸통보다 확실히 낮춘다.
 */
export const SHADED = {
  bg: '#b4c3bf',
  ground: '#aebbb4',
} as const;

/** flat 무대. 재질 도움 없이 윤곽만 본다. */
export const FLAT = {
  bg: '#ffffff',
  ground: '#dcdcdc',
  body: '#14161a',
} as const;
