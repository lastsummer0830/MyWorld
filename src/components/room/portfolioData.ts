// ⚠️ 이 파일은 껍데기다 — 내용은 `src2/content/portfolioData.ts`로 옮겼다.
//
// 이유: REBUILD_PLAN 단계 7에서 구 코드(`src/`)가 통째로 삭제되고 `src2/`가 `src/`로 승격된다.
//   포트폴리오 내용은 개편과 무관하게 살아남아야 하므로 미리 `src2/`로 옮겨 뒀다.
//   구 씬(`/`)이 아직 이 경로로 import하고 있어 다리만 남긴다. 단계 7에 `src/`와 함께 사라진다.
//
// ★ 내용 수정은 `src2/content/portfolioData.ts`에서 할 것. 여기엔 아무것도 적지 말 것.

export * from '@iso/content/portfolioData';
