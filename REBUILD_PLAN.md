# MyWorld 아이소메트릭 개편 계획 (2026-07-14 확정)

> 방향: **스택 유지(Next.js + TypeScript + Three.js/R3F), 코드 100% 새로 작성, 아이소메트릭 디오라마, 원작(myRoom, CC BY-NC 4.0) 라이선스 완전 탈피.**
> 기법 정본: **AJ_Proj 루트**의 `.claude/skills/isometric-diorama-builder` (카메라·구도·조명) + `.claude/skills/procedural-asset-styleguide` (에셋·팔레트·라이선스 탈피 규칙)
> ※ 스킬은 **repo 밖**(AJ_Proj 루트)에 둔다 — 이 repo는 Public 전환 예정이라 AI 작업 지침이 함께 공개되면 안 된다. PC 간 동기화는 감독관 CLAUDE.md §5 백업 규칙(MyStudy)으로 처리.

## 왜 이렇게 가나

- 현재 코드는 원작 뼈대의 파생물 → CC BY-NC가 따라붙어 "직접 만들었다"고 말하기 애매하다. 포트폴리오에서 이건 치명적.
- 아이소메트릭 디오라마는 ① 구도가 고정이라 순수 코드 에셋의 퀄리티를 극대화하기 좋고 ② 첫 화면에서 바로 "작품" 인상 ③ 모바일에서도 프레이밍이 안정적.
- 컨셉 자산(여름 정원·파스텔·강아지·낮밤/날씨 전환)은 조아진의 것이므로 그대로 계승한다.

## ★ 컨셉 (2026-07-13 조아진 확정 — 이게 정본이다)

**중앙은 "방"이 아니라 정원 속 쉬는 공간이다.** 아이소메트릭은 카메라 방식일 뿐, 소재가 아니다.
(※ 7/14 최초 계획서가 "코너 디오라마 + 침대·책상"이라는 **방 컨셉으로 잘못 적혀 있었다.** 7/14 조아진 지적으로 정정.)

- **무대**: 하늘에 떠 있는 **32m × 32m 정원 섬**. 잔디 뚜껑 + 흙 단면 = "땅을 도려내 온 조각".
  **판이 주인공이고 바깥 하늘·구름·부유섬은 배경**이다 — 첫 화면에서 판이 화면을 지배해야 한다.
- **중앙**: 흰 퍼걸러 + 그네벤치 + 장미덩굴, 흰 주철 비스트로 티테이블.
- **★ 포트폴리오 열람 = 테이블 위 맥북**을 들여다보는 방식. (방·책상·모니터가 통째로 불필요해진다)
- **집**: 예쁜 외관 오브젝트 정도.
- **주변**: 꽃밭, 연못 + 나무다리 + 코이, 나비, **그 나비를 보며 노는 강아지**(블루멀 셸티·오드아이 = 조아진 실제 반려견).
- **낮**: 따사롭게 내리쬐는 햇살. **밤**: 하늘빛을 거의 끄고 **반딧불이·알전구만 살린다** → Bloom 필수.
  전환은 **시차를 줄 것**: 하늘이 먼저 물들고 → 전구가 톡톡 켜지고 → 반딧불이가 피어오른다.

## 단계 (각 단계 끝 = 캡처 컨펌 게이트)

| # | 단계 | 내용 | 완료 기준 |
|---|------|------|-----------|
| 0 | 준비 | `rebuild/isometric` 브랜치, `src2/` 스캐폴드, 무대(판·하늘·카메라·낮밤·HUD) | ✅ 2026-07-14 |
| 1 | 스타일 바이블 | 팔레트·MAT 정본화 → `materials.ts` 신규. 잔디·흙·유리·천 재질 확정 | 색견본 캡처 컨펌 |
| 2 | 그레이블록 | **정원 배치도** — 퍼걸러·티테이블·연못·다리·집·꽃밭 자리를 회색 박스로 | 구도 캡처 컨펌 |
| 3 | 에셋 패스 1 | 퍼걸러 + 그네벤치 + 티테이블 + **맥북** 실제 모델링 | 각도 캡처 컨펌 |
| 4 | 에셋 패스 2 | **강아지(주인공)** · 연못/코이/나무다리 · 꽃밭 · 집 외관 · 나비 | 캡처 컨펌 |
| 5 | 연출 | 낮/밤 **시차 전환**, 알전구, 반딧불이 강화, 등장 애니메이션 | 60fps 확인 |
| 6 | 콘텐츠 | **맥북 화면에 포트폴리오 렌더**(렌더타겟), HUD 메뉴 → 카메라 연결, portfolioData | 모바일 확인 |
| 7 | 청산 | 구 코드(`src/`) 삭제, `src2/`→`src/` 승격, LICENSE 교체, README 재작성 | 원작 코드 0줄 확인 |
| 8 | 배포 | Vercel 배포, README에 라이브 링크·캡처 GIF | PORTFOLIO_STATUS 갱신 |

## 불변 규칙

- 원작 계열 코드를 열어 참조·복사하지 않는다 (스킬 §0). 위반하면 라이선스 탈피가 무효가 된다.
- push는 조아진 승인 후에만. `git add .` 금지. 단계마다 커밋은 잘게.
- 완료 선언 전 스크린샷 검증 (스킬 §7·§5). "코드만 쓰고 됐다고 말하기" 금지.

## 다음 세션 시작 지점

1. 이 파일 읽기 → 2. `npm start` 후 `localhost:3000/rebuild`로 **단계 0 결과물부터 눈으로 확인**
3. **단계 1(스타일 바이블)**: `src2/scene/materials.ts` 신규 작성 — 잔디·흙·흰 철제·유리·천·물 재질을 MAT 팩토리로.
   지금 색은 각 컴포넌트에 하드코딩돼 있다(스캐폴드라 허용). 단계 1에서 `palette.ts`로 모은다.
4. 그다음 **단계 2(정원 배치도)** — `IsoStage.tsx`의 `PROBES` 3개를 실제 배치안으로 확장.

## 스케일 정본 (단계 0에서 확정 — 이후 모든 에셋이 여기 맞춘다)

- `TILE = 1` = **1m**. `GRID = 32` → 정원 32m × 32m.
- 잔디층 0.8m + 흙 단면 1.6m.
- 카메라 부각 30°, 수평 360° 자유 회전, 휠 확대 0.22~5배.
